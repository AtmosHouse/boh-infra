# Recipe & Shopping List API Requirements
# Python 3.11+ required for type hints and other features

# FastAPI web framework
fastapi>=0.104.0

# ASGI server
uvicorn[standard]>=0.24.0

# Pydantic for data validation and settings
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Unit conversion library
pint>=0.20.0

# OpenAI API for natural language parsing
openai>=1.0.0

# YAML parsing for config files
pyyaml>=6.0

# HTTP client (async support for FastAPI)
httpx>=0.25.0

# Database
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0
psycopg2-binary>=2.9.0
